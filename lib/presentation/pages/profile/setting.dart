import 'package:flutter/material.dart';

class SettingString extends StatelessWidget {
  const SettingString({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}